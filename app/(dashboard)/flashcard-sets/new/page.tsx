import NewSet from "@/components/flashcards/NewSet";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "New Flashcards | Study AI"
}

export default function NewFlashcardsPage() {
  return <NewSet/>
}